local ENUMS = {}

ENUMS.GAME_STATE = {
	PAUSE = "PAUSE",
	RUN = "RUN",
}

ENUMS.ANIMATIONS = {
	IDLE = "IDLE",
	RUN = "RUN",
}

ENUMS.CHUNK_SIDE = {
	TOP = 0,
	BOTTOM = 1,
	RIGHT = 2,
	LEFT = 3,
	FRONT = 4,
	BACK = 5,
}

return ENUMS